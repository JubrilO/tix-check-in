query GetEvents {
    currentUser {
        events(last: 30) {
            edges {
                node{
                    title
                    startDate
                }
            }
        }
    }
}
